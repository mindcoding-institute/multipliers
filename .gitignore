# build output
dist/
.astro/

# dependencies
node_modules/

# local secrets (Turso URL + token) — never commit
.dev.vars

# scratch / local-only scripts (seed scripts, experiments)
.tmp/

# cloudflare / wrangler
.wrangler/

# logs / os
*.log
.DS_Store
