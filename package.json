{
  "name": "mindcoding-multipliers",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro build && wrangler pages dev ./dist",
    "astro": "astro",
    "deploy": "astro build && npx --yes wrangler pages deploy dist --project-name=mindcoding-multipliers --branch=main --commit-dirty=true"
  },
  "dependencies": {
    "@astrojs/cloudflare": "^12.0.0",
    "@libsql/client": "^0.15.0",
    "@webtui/css": "^0.1.3",
    "astro": "^5.6.0"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
